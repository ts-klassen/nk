# constで値に名前をつける

## 問題

`input` に入っている値を、`const` で宣言した変数 `received` に代入してください。

その後、`received` の値を `output` に代入してください。

## 入力

`input` には文字列または数値が入っています。

## 出力

`output` には、`input` と同じ値を代入してください。

## 例

`input`:

```json
"JavaScript"
```

期待される `output`:

```json
"JavaScript"
```
