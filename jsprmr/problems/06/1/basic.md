# 4で割った余り

## 問題

`input` には数値が入っています。

`input` を `4` で割った余りを、数値として `output` に代入してください。

## 入力

`input` には数値が入っています。

## 出力

`output` に、`input` を `4` で割った余りを数値として代入してください。

## 例

`input`:

```json
10
```

期待される `output`:

```json
2
```
