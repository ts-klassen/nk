# 式の評価値を代入する

## 問題

`input + 10` は、評価すると数値を生成する式です。

`input + 10` の評価値を `const` で宣言した変数に代入してください。

その変数の値を `output` に代入してください。

## 入力

`input` には数値が入っています。

## 出力

`output` には、`input + 10` の評価値である数値を代入してください。

## 例

`input`:

```json
5
```

期待される `output`:

```json
15
```
