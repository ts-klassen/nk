# 最大値と最小値の差

## 問題

数値の配列から最大値と最小値を求め、その差を計算してください。

配列の要素を `Math.max` や `Math.min` の引数として渡すときは、spread 構文 `...` を使えます。

## 入力

`input` には、1 個以上の数値を持つ配列が入っています。

## 出力

`output` に、配列の最大値から最小値を引いた数値を代入してください。

## 例

`input`:

```json
[3, 7, 2]
```

期待される `output`:

```json
5
```
