# 3倍にする関数

## 問題

数値を 1 つ受け取り、その数値を 3 倍した値を返す関数 `triple` を、関数宣言で定義してください。

そのあと、`input` を引数として `triple` を呼び出し、返り値を `output` に代入してください。

## 入力

`input` には数値が入っています。

## 出力

`output` に、`input` を 3 倍した数値を代入してください。

## 例

`input`:

```json
4
```

期待される `output`:

```json
12
```
