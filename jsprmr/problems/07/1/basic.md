# 文字列へ明示的に変換する

## 問題

`input` に入っている値を、`String` を使って明示的に文字列へ変換してください。

変換した文字列を `output` に代入してください。

## 入力

`input` には文字列、数値、真偽値、または `null` が入っています。

## 出力

`output` には、`input` を文字列へ変換した値を代入してください。

## 例

`input`:

```json
123
```

期待される `output`:

```json
"123"
```
