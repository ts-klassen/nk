# 最後のラベル

## 問題

`input` には、1 個以上の文字列が入った配列が与えられます。

配列の最後の要素を取り出してください。最後の要素のインデックスは、配列の `length` から 1 を引くことで求められます。

## 入力

`input` には、文字列の配列が入っています。

## 出力

配列の最後の要素を、文字列として `output` に代入してください。

## 例

`input`:

```json
["red", "green", "blue"]
```

期待される `output`:

```json
"blue"
```
