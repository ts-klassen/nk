# 共通認証仕様を定義する

## 目的

ユーザー登録、Basic 認証、Argon2 によるパスワードハッシュ化の仕様を定義する。

## 決定済み仕様

- Basic 認証を使う。
- JWT は使わない。
- セッション管理はしない。
- Basic 認証の ID は `username` とする。
- パスワードは Argon2 でハッシュ化して保存する。
- `POST /users` は公開 API とする。
- ユーザー更新 API と削除 API は作らない。
- `username` は一意とする。
- `username` 重複時は `409 Conflict` と `CONFLICT` を返す。
- 認証失敗時は `401 Unauthorized` と `UNAUTHORIZED` を返す。
- 認証失敗時は `WWW-Authenticate` ヘッダを返す。
- 認証済みだが権限がない場合は `403 Forbidden` と `FORBIDDEN` を返す。

## ユーザー登録リクエスト

```json
{
  "username": "alice",
  "password": "password123"
}
```

## ユーザー登録レスポンス

```json
{
  "id": 1,
  "username": "alice",
  "createdAt": "2026-04-28T11:39:55+09:00",
  "updatedAt": "2026-04-28T11:39:55+09:00"
}
```

## TODO

- [ ] `username` の文字種と長さを決める。
- [ ] `password` の長さを決める。
- [ ] Basic 認証 middleware の責務を決める。
- [ ] `WWW-Authenticate` ヘッダの realm を決める。
- [ ] 認証失敗時の message 文言を決める。
- [ ] Argon2 のパラメータを教材で説明する範囲を決める。

## 完了条件

- A/B 共通のユーザーテーブル仕様が決まっている。
- A/B 共通の認証処理を実装できる。
- API 仕様書に認証要否を明記できる。
